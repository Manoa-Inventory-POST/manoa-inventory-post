import React from 'react';
import { Col, Container, Row, Header,Image, } from 'react-bootstrap';
import { PAGE_IDS } from '../utilities/PageIDs';

/** Render a Not Found page if the user enters a URL that doesn't match any route. */
const Map = () => (


  <Container className="map-page-background" style={{ marginTop: '10px', height:"900px" }}>
  </Container>

  );

export default Map;
